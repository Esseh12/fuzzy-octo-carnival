#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 *
 */
int main()
{
	/* using loop to print numbers */

	for (int i = 1; i <= 5; ++i)
	{
		printf("%d\n", i);
	}
       return (0);
}
