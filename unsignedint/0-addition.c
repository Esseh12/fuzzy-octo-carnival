#include <stdio.h>
/**
 * main - addition of random numbers
 *
 * Return: Answer 0 (success)
 */
int main(void)
{
	unsigned int num = 500;
	unsigned int maximum = 300;
	unsigned int result = num + maximum;

	printf("Result:%d\n", result);
	
	return (0);
}
