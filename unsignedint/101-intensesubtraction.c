#include <stdio.h>
/**
 * main - intense subtraction randoms
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int num = -100;
	int max = -100;
	int result = num - max;

	printf("Result:%d\n", result);

	return (0);
}
