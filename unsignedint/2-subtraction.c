#include <stdio.h>
/**
 * main - random subtraction
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	unsigned int num = 200;
	unsigned int max = 100;
	unsigned result = num - max;

	printf("Results;%d\n", result);

	return (0);
}
