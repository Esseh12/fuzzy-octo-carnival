#include <stdio.h>
/**
 * main - Entry point of program
 *
 * Return: Always 0 (success)
 *
 */
int main(void)
{
	int age = 25, result;

	/* using the not operator */

	printf("Result_1: %d\n", !(age >=18));

	printf("Result_2: %d\n", !(age >=30));

	return (0);
}
