#include <stdio.h>
/**
 * main - Using p to get address
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c;

	printf("Address:%p\n", &c);

	char* ptr = &c;

	printf("\n%p" , ptr);

	*ptr = 

	return (0);
}
