#include <stdio.h>
/**
 * main - Trying something new everyday
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	unsigned int count = 20;
	unsigned int num = 10;
	unsigned int result = count * num;
	 printf("Results:%d\n", result);
	 
	 return (0);
}
